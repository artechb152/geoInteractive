# Design Assumptions

Visual details that were uncertain in the mockup and the assumption made while implementing.
Format: one bullet per assumption — what was unclear, what was assumed, where it's applied.

- **Progress source of truth (35% vs 4/12):** the mockup shows 35% but 4/12 = 33%. Implemented one source of truth: `percent = round(completed/12·100)`, so 4 completed ⇒ **33%**. Applied in `src/lib/course-progress.ts` / `ProgressCard`.
- **"Completed lessons" heuristic:** no real completion store exists — only the last-visit tracker (`src/lib/last-visit.ts`). Assumed `completed = lastVisit.topicNumber − 1` (lessons before the one currently visited). Fresh device ⇒ empty state: ring 0%, caption "עוד לא התחלת את הקורס", button "התחלת השיעור הראשון" → topic-01. The mockup's 35%/"4 מתוך 12" was a mid-course sample, not default content.
- **Hero primary CTA label:** stays "המשך ללמוד" always (exact mockup text, no hydration flicker); only the destination is dynamic — last-visited lesson, or topic-01 on a fresh device.
- **"סקירת הקורס" (secondary CTA):** no overview page is implied by the mockup; assumed it scrolls smoothly to the course-plan panel (`#syllabus`).
- **Progress-card button label vs destination:** mockup says "המשך לשיעור הבא" but the correct behavior (per last-visit semantics) is returning to the *last-visited* lesson; kept the mockup label with that destination.
- **Carousel behavior:** mockup shows 5 of 12 lessons with a single chevron at the inline-end (visual left). Implemented paging in windows of 5 (01–05 / 06–10 / 11–12); a back-chevron appears at the inline-start only after paging, so the default view stays pixel-identical. Forward chevron gets a disabled state on the last page.
- **"צפייה בכל השיעורים":** no all-lessons route exists; assumed the pill expands the panel in place to a 12-card grid (label toggles to "הצגה מצומצמת"). No routing added.
- **Lessons 06–12 cards:** no approved display strings in the mockup — titles use `shortTitle` from `src/lib/lessons.ts`, no subtitle. Mini-dioramas use the existing assets `lesson-{06..12}-{los,weather,logistics,chokepoints,urban,borders,gis-layers}.png`.
- **Header utility buttons:** mockup implies actionable buttons but no account/notification system exists. "התראות" opens a popover with an empty state; "החשבון שלי" opens a guest-profile popover with a working "איפוס התקדמות" action (clears last-visit after inline confirmation; hero/carousel/progress update live). Styled with approved tokens only.
- **Interactive-state styling (not in mockup):** hover = slight brightness/`paper-bright` fill lift, active = 1px press, keyboard focus = 2px `ember-soft` ring. Progress ring animates to its value (disabled under `prefers-reduced-motion`).
- **Interactive 3D hero terrain (`InteractiveHomeTerrainModel`):** the static `home-hero-terrain-mockup.png` render was replaced with a real Three.js/R3F model in the same isometric-papercut style (angle, composition, palette) — it's a stylized recreation, not a pixel-identical clone of the PNG (not achievable with real 3D geometry vs. a fixed raster crop). Colours are the approved "illustration palette" from `design/design-spec.md` §8 (terrain greens/river/edge-cream/base-rim) plus existing `ember`/token values. Two tones were deliberately derived (not new arbitrary hex) for legibility: the terrain's cream high-ground uses `--surface-card #F8F2E7` instead of the slab's own `#E8DCC4` so it doesn't camouflage against the rim it sits inside, and the river is a lerp of the documented river hex with deep sage (0.45) for a muted blue-green rather than bright blue. Falls back to the original static PNG before hydration, without WebGL, or on any runtime error (`HomeTerrainDiorama.tsx`).
- **Terrain relief rebuild (2026-07-07):** the terrain went from a flat textured plane to a real displaced/vertex-coloured mesh (`terrainField.ts`: gaussian hills → soft stacked terraces → a carved river trench → a sage/cream diagonal blend → contour-line intensity, all derived from one shared height field). The slab was rebuilt from bevelled `ExtrudeGeometry` cards instead of `<RoundedBox>` per thin layer — the RoundedBox radius (sized for a chunky single block) was far larger than each thin stacked layer's own thickness, which silently inflated into a degenerate rounded blob that visually swallowed the entire terrain mesh. Verified visually via a standalone Playwright script (the project's shared MCP browser session was locked by a concurrent session) before/after the fix.
- **Terrain rebuilt in Blender, not procedurally (2026-07-07, later same day):** a second rebuild pass (ridge/domain-warp/ridged-FBM relief, slope-adaptive carved terracing, a Catmull-Rom river channel, a two-card core+lip slab) kept surfacing the same class of bug — e.g. the terrace "groove" carving a notch at height 0 itself (there's no shelf below the ground floor to carve into), which sank the entire flat majority of the terrain below the slab's plateau and read as a flat grey/white wash in every screenshot. Rather than keep fighting hand-rolled displacement math, the same tuned design (ridge placement, terrace logic, river spline, composition anchors, palette) was ported to Python and built directly in Blender via the Blender MCP, then exported as a static `public/assets/models/home-hero-terrain.glb` (~0.75MB), loaded via drei's `useGLTF` in `InteractiveHomeTerrainModel.tsx`. `terrainField.ts` was deleted (fully superseded, no longer imported anywhere). Camera/lighting/OrbitControls/reduced-motion code is unchanged from the procedural version — only the geometry source changed.

## Phase 2 — systematizing the Home style across the site (no new HEX)

- **Semantic token remap:** the sitewide semantic tokens (`bg/fg/border/accent`) were re-pointed to the Home design-lock values that already exist in `tailwind.config.ts` — `bg` → paper.page cream, `fg` → olive ink scale, `border` → tanline scale, `accent` → ember scale, `shadow-elevated` → the Home card-soft shadow. Home components use only the `paper/olive/ember/pine/tanline` namespaces, so the Home render is unaffected.
- **Body canvas behind the Home sheet:** `body` background is now cream (`bg-bg` = paper.page), which would have hidden the Home sheet's 40px rounded corners. A fixed `paper-bright` backdrop layer was added behind the sheet in the Home `PageShell` (≈ the previous near-white canvas, existing token) to preserve the sheet-on-canvas read.
- **"White elevated cards" interpretation:** per the reference style, elevated cards stay pure white (`bg-card #FFFFFF`) on the cream page — slightly brighter than the mockup's `paper.card #F8F2E7` inner-panel cards. Hairlines are `tanline` at ~50–60% opacity; card radii keep their existing values (2xl/3xl — within the mockup's 18–24px family).
- **Decorative orange removed from backgrounds:** `TopoField` (hero-strip texture) was rebuilt in the Home contour language — tan `#C9A56B` flowing contour lines at ~0.22 opacity; the V2 sage islands and decorative orange "+" survey marks were dropped (orange = action/focus only).
- **`accent.deep` value shift:** the legacy "palest orange" token now maps to ember.soft `#D69051` (the lightest ember in the approved palette). It stays lighter than `accent.DEFAULT`, so the h1→h3 orange-lightening hierarchy is preserved, just warmer/deeper than the old `#F7CFA0`.

## Topic-01 onboarding — "4 historical stories" redesign (reference: `lesson1part2image2.png`, branch `redesign/topic01-history-stories`)

- **Panel background colour (pine vs. true charcoal) — resolved:** the reference shows a near-black/charcoal card background with almost no green cast, while the project's only "dark card" token (`pine`, `#2E3826`/`#374133`/`#283223`) reads noticeably warmer/greener. Flagged to the user rather than inventing a new hex; the user supplied a real `TOPIC01-ONB-HIST-BG.png` background asset (a near-black contour-line texture) instead, now rendered at full opacity over the `pine-grad` fallback — it visually resolves the colour gap without a new token. `bg-pine-grad` stays as the pre-asset/`src` load fallback only.
- **New copy (stat / "why it matters" / map badge / teaser lines):** the mockup's detail panel needs a bold "key fact" line and a generalized "why it matters" line per story, plus a short map badge label and a one-line card teaser — none of which existed in the original `HISTORICAL` data (only `headline`/`place`/`lesson`). All new copy was derived directly from facts already stated in each story's existing `lesson` paragraph (Napoleon's 600,000 troops / 90% losses, the Channel's 32 km, Israel's 14 km, Switzerland's unbeaten record in both world wars) — no new historical claims were introduced. Worth a pass from a Hebrew copy-editor before shipping.
- **Illustration assets:** every map/photo slot (4 story maps + 1 panel background texture) is wired through the existing `IsometricAsset` placeholder pipeline (`assetId`/`src` under `/assets/lessons/topic01/scene-onboarding/`), matching the convention already used in `AsymmetricScene.tsx`. Until the real PNGs are dropped at those paths, they render as the standard magenta "PLACEHOLDER" pattern (intentional, per that component's own doc comment) — the panel-background one is dimmed to ~16% opacity + `mix-blend-luminosity` so it reads as a faint texture rather than a wall of magenta.
- **Card-strip mini-map aspect (`21/9`) vs. detail-panel map aspect (`16/9`):** picked from `IsometricAsset`'s existing enum (`16/9 | 21/9 | 1/1 | 4/3`) as the closest match to the mockup's very-wide card thumbnail and the roughly 2:1 detail map — not pixel-exact; revisit once the real assets land and their natural crop is known.
- **Default open story:** defaults to story `01` (Napoleon) on load, matching the vertical `STEPS` accordion above it always opening its first item. The mockup screenshot happens to show story `02` (the Channel) expanded, which reads as an arbitrary example state, not the intended default.
- **Card footer teaser line:** the mockup shows a muted one-line hook under each collapsed card, below a divider, separate from the place/date caption right under the map. Kept both lines (place caption, then divider, then teaser) inside the same card rather than treating the teaser as a separate structure — the two read as visually distinct rows either way.
- **Bottom nav / "עכשיו אתם מוכנים" callout untouched:** the mockup's dark footer buttons ("הבא: רמות מלחמה" / "הקודם: פתיחה") and page counter are existing `PagedLearn` scene-level chrome already rendered by the app shell, not part of this section — left as-is, not rebuilt.
- **Round 2 fixes (2026-09-02), from direct user review against the same reference image:**
  - **"Logos" the user asked to delete** — confirmed via clarifying question: the small icon-in-circle badges (anchor/mountain/etc.) inside the stat box and the "why it matters" box. Both boxes are now plain text (heading/paragraph only); the per-case `icon` field and the `Icon`/`IconName` import were removed from `HistoricalCasesPanel.tsx` since nothing consumes them anymore.
  - **Card thumbnails now use the `-PREVIEW.png` assets** the user generated (`TOPIC01-ONB-HIST-{NAPOLEON,CHANNEL,ISRAEL,SWITZERLAND}-PREVIEW.png`) instead of reusing the full detail-panel map image at a squashed aspect ratio — wired via new `previewAssetId`/`previewSrc` fields per case. The full (non-preview) asset is still used for the larger detail-panel map.
  - **Card header/body reordered to match the mockup:** number bumped from 24px to 40px (pushes the title into a tighter, more-wrapped column, per the mockup); title colour is white/cream in both active and inactive states (only the number and the place/date line turn orange-family when active — the mockup does *not* recolor the title); place/date line moved to directly under the title, above the map thumbnail (was below it); the divider between thumbnail and teaser was dropped (not present in the reference).
  - **Active-card → detail-panel pointer:** the mockup shows a small triangular "tail" hanging off the bottom of the active card (ember-coloured) and a second one on the top edge of the cream detail panel below it, both converging on the active progress-dot. Implemented as two small `clip-path` triangles, horizontally positioned via `insetInlineStart: (activeIndex + 0.5) / count * 100%` (a fractional estimate of each card's centre, not a measured DOM position) — reads correctly in the screenshot but hasn't been pixel-verified against the connector-dot row's own (pre-existing, unchanged) flex layout, which distributes dots by wrapper-start rather than card-centre. Worth a closer look if the two ever visibly diverge.
  - **"Leave room to see the background" note:** no code change made — the card thumbnail already sits inset within the card's own padding (`p-3.5`) on a translucent card fill (`bg-white/[0.04]` / `bg-ember/20`), so the panel background shows through around every thumbnail already. Revisit once the user's real map-textured background asset replaces the current `TOPIC01-ONB-HIST-BG.png` placeholder, in case the new asset's contrast needs the thumbnail inset adjusted.
- **Round 3 fixes (2026-09-03), from direct user review against the same reference image, via `/reference-to-ui-exact`:**
  - **Detail-panel map no longer bleeds edge-to-edge:** the map (`min-h-[220px]` column) now sits in a `p-4 md:p-5` inset with its own `rounded-xl overflow-hidden`, instead of `absolute inset-0` filling the whole cell. The reference's own screenshot isn't at a clean 1440px frame (1734×907px, a prototype-export scale, not this project's `1440px` target viewport), so the exact reference px gap couldn't be lifted 1:1 — `p-4 md:p-5` (16–20px) was chosen from the existing spacing scale as the closest visual match and confirmed by side-by-side screenshot at 1440px. Revisit only if a future pixel-exact mockup at 1440px specifies a different value.
  - **Card-tail triangle / progress dot / detail-panel tail triangle now share one geometric source of truth:** this closes the gap flagged in Round 2 above (arrow position was "a fractional estimate... not a measured DOM position"). The progress-dot row and the panel's pointer-tab were rebuilt as overlay `<div>`s using the exact same `grid grid-cols-2 gap-3 sm:grid-cols-4 md:gap-3.5` template (and zero extra horizontal padding) as the card grid itself, so each dot/tab's grid cell is pixel-identical to its card's cell — no percentage-of-row-width formula involved anymore. Verified at both the default active card (01) and a clicked card (02, matching the reference's own example state) via Playwright screenshots at 1440px; both show the down-triangle, dot, and up-triangle on the same vertical line.
  - **Active progress dot enlarged:** was a flat `size-2` regardless of state; the active dot is now `size-3.5 bg-ember ring-4 ring-ember/20` (others stay `size-2`, ember if before the active index, muted white/15 after — cumulative-progress coloring preserved unchanged, only the active dot's size/ring is new) to match the reference's visibly larger solid circle.

## Topic-01 hook scene — paper/olive/ember redesign (reference: `lesson1part1image1.png`, branch `redesign/topic01-history-stories`)

- **Scope confirmed with user:** the reference screenshot also shows a dark-green top nav and a right-side lesson TOC, but both are shared components used site-wide (`AppHeader`, `ScenePagerDesktop`). Asked the user directly rather than assuming; they confirmed scope is the hook scene only (card + background), leaving the existing cream header and TOC untouched.
- **Background asset:** the user's generated PNG (`ChatGPT Image Sep 3, 2026, 10_00_02 AM.png`, 1820×864 — nearly identical aspect to the reference's 1822×863) was renamed to `TOPIC01-HOOK-BG.png` and moved into a new `public/assets/lessons/topic01/scene-hook/` folder, matching the sibling `scene-onboarding/` naming convention instead of keeping spaces/commas in the filename. Applied as a plain CSS `background-image` (not the `IsometricAsset` placeholder pipeline) because that component's `position` prop only supports `center/top/bottom`, not the explicit "hug the inline-start edge" anchor this crop needs — `bg-[position:left_center]` keeps the map from being centre-cropped under the card.
- **H1 orange split:** the pre-existing hook copy wrapped the *entire* second line in the accent color; the reference only colors the trailing "שמכריע אותה." clause, so the span was narrowed to match — no wording changed, only which words carry the ember color.
- **Card sizing tuned by screenshot, not measured px:** the reference isn't at a clean, measurable frame (full-page screenshot, not a 1:1 design export), so card `max-w` and the H1 clamp ceiling were chosen iteratively via Playwright screenshots at 1440px until the H1's second line stopped wrapping to a third line, matching the reference's two-line hero exactly.
- **Colors reused, not sampled fresh:** eyebrow/CTA use `ember`, H1/divider-icon use `olive.ink`, paragraph uses `olive.soft`, hairlines are `tanline`, CTA fill/shadow are `bg-cta-ember`/`shadow-cta-ember` — all pre-existing tokens from `tailwind.config.ts` (the same palette approved for `design/mockup.png`), no new hex introduced.
- **Round 2 fixes (2026-09-03), from direct user review — background must fill the full screen, card must be transparent, block shifted right:**
  - **Full-screen background, no site-bg bleed:** the section's `min-h` was `calc(100dvh-var(--header-h)-5rem)` — copied from other sub-topic scenes, where the extra `-5rem` reserves room for the prev/next nav row `PagedLearn` renders below them. The hook scene hides that row entirely (`isHook` in `PagedLearn.tsx`), so the `-5rem` just left an uncovered strip at the bottom of the page showing the global `SiteBackground` parchment texture through `body`'s `min-h-screen`. Dropped the `-5rem` so the section (and its background) fills exactly the viewport height below the header, edge to edge.
  - **Card background made transparent:** removed `bg-paper-card` + `shadow-card-soft` from the text block per direct user correction — the reference's "card" reads as text sitting directly on the photo backdrop, not an opaque surface. Also removed the `bg-paper-card` mask that sat behind the divider's crosshair icon (no longer needed without a card fill).
  - **Text block shifted toward the sidebar (inline-start/right in RTL):** was `justify-center`, which — once the card lost its opaque fill — left body text overlapping the darker map art on the left. Changed to `justify-start` (packs to the *inline-start* edge, which is the right side in this RTL layout) with an asymmetric `ps-*`/`pe-*` gap, and narrowed the card (`max-w-[600px]`, paragraph capped at `max-w-md`, H1 clamp ceiling lowered) so the whole text block sits inside the cream part of the backdrop instead of spanning into the terrain art, matching the reference's proportions (card occupies roughly the right ~60% of the content column, not centered).
  - **Recurring Windows dev-server HMR bug (unrelated to this component):** every edit during this round intermittently 500'd every route with `Runtime TypeError: __webpack_modules__[moduleId] is not a function` (a stale/corrupted incremental build, not a code error — `tsc --noEmit` stayed clean throughout). Worked around each time by killing the dev server, deleting `.next`, and restarting — not caused by and not fixed in this component's code.
- **Round 3 fixes (2026-09-03), from direct user review — page still scrolled, background still showed the site's parchment texture:**
  - **Root cause found via Playwright DOM measurement, not guesswork:** at 1440×900 the page was scrolling by exactly 80px (`document.documentElement.scrollHeight` 980 vs `window.innerHeight` 900). Traced with `getBoundingClientRect()` up the DOM: `LessonShell.tsx`'s sticky sub-nav row (`בדיקת ידע`/`תרגול`/`לימוד`, a `<header className="sticky top-[var(--header-h)] ...">`) is 32px tall, plus the `<main>` wrapper's `py-5 md:py-6` adds 24px above *and* below the scene content — 32+24+24 = 80px of shared-chrome height the Round-2 `min-h-[calc(100dvh-var(--header-h))]` formula never accounted for (it only subtracted the header). That 80px is exactly what the *original* pre-redesign formula's `-5rem` was already compensating for — removing it in Round 1 (to fix the "site background visible below the card" gap) was the wrong fix for that bug, since it just traded a coverage gap for a forced scrollbar.
  - **Real fix — decouple the background from section height entirely:** the backdrop div is now `fixed inset-0` (was `absolute inset-0`) instead of trying to make the in-flow `<section>` exactly match the remaining viewport height via ever-more-precise `calc()` subtraction (fragile — breaks again the moment shared chrome changes height at another breakpoint). A `fixed` layer always covers the full `100dvh` viewport regardless of the section's own height, scroll position, or any ancestor padding, so the `min-h` formula could go back to `calc(100dvh-var(--header-h)-5rem)` (fits exactly under the sticky tabs row + main padding, zero page overflow) while the image still paints behind that chrome too — no more gap showing the site's default parchment texture anywhere on this scene. Verified with a Playwright script that reads `document.documentElement.scrollHeight === window.innerHeight` (true, no scrollbar) and re-reads the backdrop's `getBoundingClientRect()` after a forced `scrollBy()` to confirm it stays pinned at `(0,0)`–`(1440,900)` — i.e. genuinely viewport-fixed, not accidentally trapped in a `transform`-ed ancestor's containing block (a real risk here since `PagedLearn.tsx` wraps every scene in an animated `motion.div`, which framer-motion could leave a non-identity `transform` on).

## Topic-01 levels scene — top card rebuilt as an interactive level selector (reference: `lesson1part3image1.png`, branch `redesign/topic01-history-stories`)

- **2026-09-03, `/reference-to-ui-exact`:** replaced `LevelsScene.tsx`'s top `surface-elevated` card (previously a static pyramid diagram + paragraph, then a 3×4 `ComparisonMatrix` table) with a two-column selector: `TOPIC01-LEVELS-DIORAMA.png` with 3 overlay buttons on the visual left, an active-level detail panel (crossfaded via `framer-motion`, `AnimatePresence mode="wait"`) on the visual right — first child in DOM per this codebase's RTL convention, matching `HistoricalCasesPanel.tsx`'s own pattern. Defaults to `strategic` on load.
- **Pyramid + paragraph deleted, not relocated:** user-confirmed it isn't in the reference and doesn't need to be preserved elsewhere. `SVGPyramidStatic` and `ComparisonMatrix` (the old table renderer) were both removed from `LevelsScene.tsx` after confirming via repo-wide grep that neither was imported/used anywhere else.
- **Single-color-active, deliberately diverging from this same file's tri-color identity:** the drag-and-drop practice section below (untouched, out of scope) still colors each level distinctly (`accent-intel` purple / `accent` orange / `terrain-sand` sand, via `LEVELS[level].text`/`.borderActive`/`.bgActive`). The new selector above it, per direct user correction against the reference, uses only ONE accent color (`accent`, i.e. orange/ember) for whichever level is currently active; the other two buttons stay a neutral dark treatment (`bg-fg/80 text-bg-elevated`, reusing the existing `bg-fg text-bg-elevated` "neutral selected" pattern already used elsewhere in this codebase, e.g. `AsymmetricScene.tsx`). This is intentional and should not be "fixed" back to tri-color without re-checking this decision — also noted in `docs/UI-CONSISTENCY-RECOMMENDATIONS.md`.
- **Right-panel content diverges from the reference's own layout, per explicit task correction:** `lesson1part3image1.png` shows all 3 levels stacked as a list in the right panel; the approved brief instead specifies the panel show *only* the active level's full info (label/english + all 4 `MATRIX_ROWS` rows as subheading/paragraph pairs), matching the "click a button → one detail panel crossfades" pattern already used by `HistoricalCasesPanel.tsx`. No copy was changed — all strings reused verbatim from the existing `LEVELS`/`MATRIX_ROWS` data.
- **Diorama aspect (`3 / 2`, not one of `IsometricAsset`'s four fixed options):** the asset is exactly 1536×1024 = 3:2, which sits between the `4/3` (1.33) and `16/9` (1.78) presets and isn't a close match to either — used a custom `style={{ aspectRatio: '3 / 2' }}` container with `IsometricAsset`'s `className="... [aspect-ratio:auto]"` override instead, the same technique `HistoricalCasesPanel.tsx` already uses for its own detail-panel map column. Result: the full asset renders with zero cropping.
- **Button placement tuned by eye against the rendered asset, not the reference photo:** `lesson1part3image1.png`'s photo has a different composition than the actual `TOPIC01-LEVELS-DIORAMA.png` (this project's own generated asset), so positions were picked by looking at the rendered diorama itself — top-left "war room" vignette for strategic, the mid-frame control-room-with-screens for operational, the bottom-right close-up vignette for tactical — via `insetInlineEnd`/`insetBlockStart` (distance from the visual left/top, expressed as logical properties so nothing needs mirroring), then refined over 2 iterations of Playwright screenshots at 1440px until each button sat on its own region. A named, remaining limitation: these are eyeballed percentages tuned against one render, not measured/pixel-exact — if the diorama asset is ever regenerated with a different composition, the button positions will need re-tuning.

## Open question — topic-01 STEPS accordion sizing (found during `/checking-design-fidelity` review, 2026-09-03)

- **Unresolved, left as-is pending a decision:** the STEPS accordion in `OnboardingScene.tsx` (the "לפני שמתחילים" step panel) is the file the project skill (`.claude/skills/checking-design-fidelity/SKILL.md` Table 2) names as the canonical lesson-scene-content reference, and 15 other files across topics 02–12 (`OnboardingScene.tsx` in each, plus `BufferScene.tsx`, `LOCScene.tsx`, `CombatNavScene.tsx`, `EngineeringScene.tsx`) were explicitly unified to match its exact classes. In the current working-tree diff, this file's own accordion shrank below those documented values with no accompanying note here: heading/sub-heading/body `text-base`→`text-sm`, number badge `size-9`→`size-8` (check icon 16→14, digit `text-sm`→`text-xs`), trigger padding `p-4`→`p-3` (`gap-3`→`gap-2.5`), expanded-content padding `px-4 pb-4 pt-1`→`px-3 pb-3 pt-1`, sub-heading `mb-1.5`→`mb-1`. Asked the user directly whether this is an intentional resize that should become the new standard (propagate to the other 15 files) or a regression to revert; answer was "not sure — leave as-is for now, decide later." Until decided, topic-01's accordion visually reads smaller/denser than the identical accordion pattern everywhere else on the site.
