# פרומפט לקלוד קוד — מפת הלחצים

עבוד בפרויקט הקיים:

`C:\Users\idog2\Desktop\Programming\Artech\geoInteractive`

המשתמש אישר את המוקאפ הריאליסטי הבהיר ואת רעיון האינטראקציה. המשימה היא לממש את הרכיב בתוך השיעור הראשון, במקום ציר הזמן הקיים בחלק ״למה הזמן הוא הנשק הסודי של השחקן הלא-סדיר?״. השלם מימוש ובדיקה מקומיים. אל תפרסם ואל תעשה commit ללא בקשה.

## קרא תחילה

1. `AGENTS.md` וההוראות הרלוונטיות בפרויקט.
2. `design\handoff\asymmetric-time-v3\IMPLEMENTATION-SPEC.md` — ההתנהגות, הפריסה והבדיקות.
3. `design\handoff\asymmetric-time-v3\interaction-content.json` — הנוסח המדויק והנתונים לכל הסבבים.
4. `design\handoff\asymmetric-time-v3\asset-manifest.json` — הנתיבים המלאים, גדלי התמונות ועוגני המפה.
5. פתח והצג את `C:\Users\idog2\Desktop\Programming\Artech\geoInteractive\design\mockups\asymmetric-time-realistic-v3.png` — המוקאפ המאושר.
6. פתח את התמונות עצמן לפני הצבת התוויות. המוקאפ הוא מקור הכיוון החזותי; הנתונים במסמכי החבילה מתקנים שגיאות טקסט שנוצרו בתמונת המוקאפ.

## גבולות המשימה

נקודת החיבור שנמצאה: `src\components\lessons\topic-01\AsymmetricScene.tsx`, קריאת `<TimeAsymmetry />` באזור שורה 367, והגדרת `function TimeAsymmetry()` באזור שורה 1055. אתר לפי שמות ותוכן כי מספרי השורות עשויים להשתנות.

החלף את רכיב הזמן בלבד. צור רכיב מקומי נפרד, למשל `TimePressureExperience.tsx`, וקובץ נתונים מתאים. בדוק שימושים לפני מחיקת הקבועים וה־TimelineScrubber הישנים. אל תשנה רכיבים אחרים בסצנה, ניווט שיעור, כותרת עליונה, סרגל צד, יחידות אחרות, או את עיצוב האתר הכללי. מעטפת האתר שבתמונת המוקאפ היא הקשר בלבד. אל תעתיק ממנה תיבת חיפוש, לחצני זום או קישורי ניווט שאינם קיימים באתר.

## התמונות כבר נמצאות במחשב

מקור:

`C:\Users\idog2\Desktop\Programming\Artech\geoInteractive\design\handoff\asymmetric-time-v3\assets`

יעד לשימוש באתר:

`C:\Users\idog2\Desktop\Programming\Artech\geoInteractive\public\assets\lessons\topic01\scene-asymmetric\time-pressure`

העתק את שלוש התמונות לפי `asset-manifest.json`. צור רק את תיקיית היעד אם היא חסרה. השאר את המקור. קובץ יעד שכבר קיים וזהה ניתן להשאיר; אם תוכנו שונה, שמור גרסה בשם חדש ועדכן את ההפניה — אל תדרוס נכס קיים ללא בדיקה. אפשר להשתמש ב־`copy-assets.ps1` שבחבילה; הוא בודק hashes ולא דורס קובץ שונה.

השתמש באתר בכתובות שמתחילות ב־`/assets/lessons/topic01/scene-asymmetric/time-pressure/`, לא בנתיבי Windows ולא ב־file://. שלוש התמונות הן כל נכסי הרסטר הנדרשים לרכיב. אל תשתמש במוקאפ כתמונת הרכיב. את התוויות, הקווים, הסמלים, הרשת הקטנה, הכפתורים, מסגרות הבחירה והמשוב יש ליצור ב־HTML/CSS/SVG עם הרכיבים הקיימים. אין צורך בתלת־ממד חי או ב־Three.js.

הפרויקט משתמש ב־static export וב־`NEXT_PUBLIC_BASE_PATH`. הוסף את קידומת ה־basePath לכתובת התמונה בדיוק פעם אחת, לפי הדפוס הקיים ב־`IsometricAsset.tsx`. אין לשנות רכיב משותף רק כדי להוסיף יחס תמונה: הרכיב הקיים אינו תומך כרגע ב־3:2; אפשר להשתמש ב־img מקומי עם width/height ויחס המקור. ודא פעולה גם בייצוא סטטי ל־LMS/SCORM, ללא כתובות רשת חיצוניות או שירות תמונות.

## כיוון חזותי מאושר

מראה ריאליסטי באור יום, צבעי חומר טבעיים, קרם ומשטחים בהירים. כתום האתר `#D97E2B` לפעולות ולמוקד האינטראקציה. אין פילטר כתום על התמונות, אין רקע ירוק כהה, ואין מראה של מודל חימר. צבעי הצילום עצמם נשארים טבעיים. השתמש בטוקנים, בגופני Rubik/Heebo ובכרטיסים הקיימים. שמור טקסט גוף קריא של לפחות 16px. המשתמש אישר את שינוי העיצוב בחלק הזה; אין להרחיב אותו לשאר האתר.

## ליבת ההתנהגות

שלושה סבבים: פתיחת המערכה → הלחץ מצטבר → תמונת המצב. בכל סבב: אירוע קצר, שתי תחזיות אפשריות, בדיקת התחזית ומשוב סיבתי. ההדגשה החדשה במפה מופיעה רק לאחר הבדיקה, ולא מסגירה את התשובה מראש. לאחר המשוב אפשר להמשיך גם אם הבחירה שגויה; ההסבר הוא חלק מהלמידה. שמור תשובות ומצבי משוב בעת חזרה לסבב קודם, ואל תפעיל מחדש אנימציה של משוב שכבר נצפה. בסוף הצג שאלת העברה קצרה עם טענה וראיה, ואז תובנה מסכמת. כל התוכן והכללים נמצאים בשני קובצי המפרט.

המפה מציגה לחצים אפשריים, לא הסתברויות או מדדי כוח מדויקים. אין להחזיר את מסר ״5 חזיתות מול 1״, אין מד ״חיים״ לשחקן לא־סדיר, ואין טענה שהזמן תמיד מבטיח לו ניצחון. זו המחשה שתלויה ביעדים, במשאבים ובתנאים.

## בדיקה והשלמה

התאם ל־1440px בתוך מעטפת השיעור בפועל. הצג את הרכיב במצב לפני תשובה, אחרי תשובה נכונה, אחרי תשובה שגויה, בסיום, ובחזרה לסבב קודם. בדוק את כל המעברים, מקלדת, RTL, מיקומי התוויות, תנועה מופחתת וטעינת התמונות. אין להפוך או לשקף את המפה עבור RTL. הרץ את הבנייה והבדיקות הזמינות המתאימות בפרויקט; אל תניח שקיים סקריפט test או ש־next lint נתמך רק מפני שהוא כתוב ב־package.json. דווח מה באמת הורץ.

לפני סיום השווה צילום מסך ב־1440px למוקאפ, תקן חפיפות וחיתוכים, וודא שהסצנה לפני ואחרי הרכיב לא השתנתה. מסור סיכום קצר: קבצים ששונו, תמונות שהועתקו, בדיקות שבוצעו ומגבלות שנותרו. אל תטען שהמוקאפ או חבילת התמונות הם מימוש עובד — המשימה שלך היא לבנות אותו.
