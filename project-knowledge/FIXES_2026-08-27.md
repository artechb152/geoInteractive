# תיקונים 27.8.2026

מקור: רשימת תיקונים שהועברה ע"י המשתמש בתאריך 27.8.2026. פורמט תואם ל-[PROJECT_FIXES_TODO.md](PROJECT_FIXES_TODO.md) — סטטוס `[ ]`/`[x]` + הערת יישום בתום הטיפול. לכל סעיף הוגדרה "הצלחה" — קריטריון בדיקה ברור לפני סימון כ-DONE.

---

## כללי

- [x] **שינוי מספור שיעורים — ניווטים עובר לשיעור 6**
  היום: `topic-03` = ניווטים (אזימוט ותכנון ציר). לשנות כך ש**ניווטים יהיה שיעור 6**, ולהקדים ב-1 את כל השיעורים שביניהם (הקרטוגרפיה/טופוגרפיה/ניידות/LOS שהיו 04–06 יהפכו ל-03–05).
  **הצלחה:** תפריט השיעורים, `lessons.ts`, `quizzes.ts`, תיקיות הקבצים ו-URL/slug של כל שיעור מוצגים במספור החדש; שיעור ניווטים מופיע במיקום 6; אין קישור שבור בין שיעורים (recap/next-lesson); `tsc --noEmit` ו-`next build` עוברים נקי.
  **יישום (2026-08-27):** בוצע רוטציה פיזית (דרך תיקיית temp) של 4 הנושאים על כל הקבצים הקשורים: `src/components/lessons/topic-0[3-6]/` (תיקיות + שם קובץ ה-Lesson + שם הפונקציה המיוצאת), `content/topic-0[3-6].md` (כולל תיקון שורת "נושא N:"), `public/assets/isometric/lesson-0[3-6]-*.png`. עודכנו `src/lib/lessons.ts` (סדר המערך + `id`/`number` + `DIORAMA_SLUGS`), `src/lib/quizzes.ts` ו-`src/lib/lesson-scenes.ts` (מיפוי מחדש של מפתחות `topic-0[3-6]`), ו-`CoursePlanPanel.tsx` (`APPROVED_TITLES`). `src/app/lessons/[topicId]/page.tsx` לא נדרש עדכון — הוא גנרי לפי `topic-01..12`. תוקן גם `src/components/design-approval/placeholders.tsx` שהיה מפנה לנתיבי-נכס ישנים (נמצא רק אחרי grep sweep נוסף — לא היה חלק מה-inventory המקורי). אומת ב-`tsc --noEmit`, `next build` (כל 12 ה-routes), ובדפדפן (Playwright): קרוסלת הבית מציגה 06=ניווטים/03=מורפולוגיית שטח, וקישור "השיעור הבא" בסיכום שיעור 03 מוביל נכון ל-topic-04.

- [x] **Hover על שם שיעור במסך הבית — תצוגת ראשי פרקים**
  לבדוק היתכנות: כשעומדים עם העכבר על שם שיעור במסך הבית, להציג לצידו את רשימת ראשי הפרקים/הסצנות של אותו שיעור, כדי לא להיכנס לשיעור רק כדי לראות את הנושאים.
  **הצלחה:** מסמך היתכנות/החלטה קצר (אפשר בעיצוב Tooltip/Popover); אם ממומש — hover על כרטיס שיעור במסך הבית מציג רשימת נושאים תוך פחות מ-300ms, לא חוסם קליק לכניסה לשיעור, ותקין ב-RTL ובמקלדת (focus/keyboard-accessible).
  **יישום (2026-08-27):** ממומש כ-overlay CSS-בלבד (ללא JS/state חדש) בתוך `LessonCard` המקומי ב-`CoursePlanPanel.tsx`: שכבה `absolute inset-0` בתוך גבולות הכרטיס עצמו (לא חורגת מהם — כדי לא להיחתך ע"י ה-`overflow-x-auto` של שורת הקרוסלה), עם `pointer-events-none` כך שקליק תמיד עובר ל-`<Link>` שמתחתיה, ו-`opacity-0 → group-hover/hover:opacity-100 / group-focus-visible/hover:opacity-100` על אותו `group/hover` שמוגדר על ה-`<Link>` עצמו — כך ש-Tab במקלדת מפעיל בדיוק את אותה שכבה כמו hover בעכבר. רשימת הפרקים מגיעה מ-`lessonScenes` (`@/lib/lesson-scenes`), בסינון סצנות המסגרת (`hook`/`onboarding`/`recap`). אומת בדפדפן: הרקע האטום (`bg-olive-ink/95`) והטקסט קריאים במלואם, קליק על כרטיס בזמן hover עדיין מנווט לשיעור, ו-`document.activeElement.matches(':focus-visible')` + `opacity:1` אומתו יחד אחרי Tab אמיתי.
  **הערה טכנית:** נתקלנו בבאג לא-מובן-מאליו של Tailwind — מודיפייר האטימות `/92` (למשל `bg-olive-ink/92`) לא נוצר כלל ב-CSS כי 92 אינו אחת מנקודות-העצירה המוגדרות בסולם `theme.opacity` המובנה (`10,50,90,95,100`...); הוחלף ב-`/95` שכן נתמך. שווה לזכור לעבודות עתידיות עם ערכי אטימות "עגולים אבל לא בדיוק בסולם".

---

## שיעור קרטוגרפיה (topic-02)

- [x] **קואורדינטות — הסבר לימודי מפורט על ITM + תרגול דקירה**
  להוסיף הסבר מעמיק על משמעות כל ספרה בנ.צ ITM (מה כל זוג ספרות מייצג, רזולוציה של 6/8/10 ספרות וכו'), ולהוסיף תרגיל "דקירת נ.צ" על מפה — המשתמש מקבל הדרכה איך עושים ואז מתרגל בעצמו.
  **הצלחה:** קיים הסבר טקסטואלי/ויזואלי שממפה כל ספרה/זוג-ספרות בנ.צ למשמעותה (מזרחה/צפונה, רמת דיוק); קיים תרגיל אינטראקטיבי שבו לוחצים/מסמנים נקודה על מפה ומקבלים בדיקת נכונות של הנ.צ שחושב; יש שלב הדגמה (walkthrough) לפני התרגול העצמאי; נבדק ב-tsc ובדפדפן.
  **יישום:** נוסף פסקת "קריאת נ"צ: מה כל ספרה אומרת" ב-`content/topic-02.md` (אחרי פסקת נקודת ציון), וב-`CoordinatesScene.tsx` נוספו `DigitAnatomy` (פירוק ויזואלי של הדוגמה לספרות ק"מ מודפסות מול ספרות-דיוק שנמדדות במדקו) ו-`GridReferenceExercise` (טאבים "הדגמה"/"תרגול עצמאי": `DigitWalkthrough` מדגים שלב-אחר-שלב על משבצת ק"מ אחת, `DigitPractice` מריץ 3 תרגילי "דקירת נ.צ" עם משבצת-לחיצה 10×10 נגישה-מקלדת שמחשבת את הנ"צ של התא שנלחץ ובודקת נכונות מול היעד). תוך כדי כך תוקן באג עובדתי קיים: דוגמת ה-ITM ב-`SYSTEMS.itm.example` הייתה הפוכה (666250/178350 — צפון/מזרח, לא מזרח/צפון); תוקנה ל-178350/666250 בהתאם לטווחי ה-easting/northing האמיתיים של ITM. תוכן עבר בדיקת דיוק (military-geo-editor) ועריכה לשונית (hebrew-copy-editor) — תיקון אחד עלה משתי הבדיקות: הניסוח "מצמצם את השטח פי 10" היה שגוי מתמטית (זו טעות שטח מול קו — תוקן ל"מדייק את המיקום פי 10 בכל ציר בנפרד"). נבדק: `tsc --noEmit` נקי, `qa:rtl` לא מוסיף חריגות חדשות (2 השורות שנשארו הן קוד ישן ב-`ImpactMap` שלא נגעתי בו), וגיאומטריית ה-SVG של המשבצת אומתה ברינדור מבודד (`qa:svg`/sharp) לפני ואחרי תיקון חפיפה בין תוויות הצירים בפינה. **לא הושלם:** בדיקת דפדפן חיה (Playwright) נחסמה — דפדפן ה-MCP המשותף היה תפוס ע"י session מקביל אחר במשך כל העבודה; מומלץ צילום מסך ידני/Playwright בהזדמנות הראשונה לפני שילוח ל-production.

---

## שיעור טופוגרפיה ומורפולוגיית שטח (topic-04)

- [ ] **תבניות נוף — "שקע" → "מכתש"**
  להחליף את המונח "שקע" ב-"מכתש" בכל מקום שמופיע בהקשר תבניות הנוף.
  **הצלחה:** אין אזכור של "שקע" כתבנית-נוף בקובץ הסצנה/תוכן הרלוונטי (`LandformsScene.tsx` + `content/topic-04.md`); כל האיורים/תוויות שמתייחסים לצורה מציגים "מכתש"; military-geo-editor מאשר שהמונח מדויק בהקשר.
  **Not applied — military-geo-editor rejected the term.** Implemented the swap in all 5 landform-context files (`LandformsScene.tsx`, `content/topic-04.md`, `RecapScene.tsx` glossary, `lessons.ts` objective, `quizzes.ts` q2), then ran the required military-geo-editor confirmation per the success criterion. Verdict: **מכתש is factually wrong here** — it's the specific term for crater-like formations (Negev makhteshim / impact craters), not the generic closed "depression" landform, which is correctly called **שקע** in Israeli map-reading/IDF terrain doctrine. Reverted all 5 files back to "שקע" (user confirmed via prompt on 2026-08-27). If "מכתש" was still intended, it needs a different justification/source before re-applying — this item stays open pending that.

---

## שיעור GIS מבצעי (topic-12)

- [ ] **"לפני שמתחילים" — ניסוח לא תקין**
  ניסוח ההסברים בפתיחת השיעור (מפה, GIS, ניתוח...) לא כתוב כמו שצריך — לנסח מחדש.
  **הצלחה:** נוסח חדש עבר עריכה ע"י hebrew-copy-editor ואישור תוכן ע"י military-geo-editor; אין משפטים חתוכים/לא ברורים; נבדק חי בדפדפן שאין חפיפה/חיתוך.

- [ ] **הסבר מעמיק: ראסטר, אורתופוטו, קואורדינטות**
  כרגע יש רק דוגמה ויזואלית. להוסיף הסבר טקסטואלי/מושגי לעומק על כל אחד מהמושגים ולא רק להסתמך על ההמחשה.
  **הצלחה:** לכל מושג (ראסטר / אורתופוטו / קואורדינטות) יש פסקת הסבר עצמאית שמגדירה את המושג ולא רק מציגה דוגמה; ההסבר עבר בדיקת דיוק תוכן (military-geo-editor).

- [ ] **טבלת תכונות — הבהרת המטרה**
  החלק שמסביר את טבלת התכונות (attribute table) לא ברור מה הוא מנסה להעביר.
  **הצלחה:** נוסח מחדש שמסביר בבירור מה זו טבלת תכונות ולמה היא רלוונטית לניתוח מבצעי, מאושר ע"י instructional-designer שהוא מתקשר למטרת הסצנה.

- [ ] **משטח עלות — הסבר על "עלות" (LCP)**
  אין הסבר על המשמעות של "עלות" ב-Least Cost Path (LCP) — המונח מופיע בלי פירוש.
  **הצלחה:** נוסף הסבר מפורש למושג "עלות" ב-LCP (מה מרכיב את ציון העלות: שיפוע/מכשולים/איומים וכו') ומה זה LCP בפועל; מאושר ע"י military-geo-editor.

---

## סימולטור שטח תלת מימדי (topic-01 / TerrainCanvas)

- [ ] **הוספת אזימוט טווח**
  להוסיף תצוגת/מדידת אזימוט וטווח (מרחק) בתוך הסימולטור התלת-ממדי.
  **הצלחה:** בזמן ניווט/תצפית בסימולטור, מוצג אזימוט (0–360°) וטווח למטרה/נקודה נבחרת בזמן אמת, מתעדכן עם תזוזת המצלמה/השחקן.

- [ ] **ניווט מודרך — תזוזה חופשית עם העכבר בתוך שטח הניווט**
  כרגע (ככל הנראה) התנועה בניווט מודרך מוגבלת. לאפשר תזוזה חופשית בתוך שטח הניווט באמצעות העכבר.
  **הצלחה:** בתוך מצב "ניווט מודרך" ניתן לזוז/לסובב מצלמה בעכבר בכל שטח הניווט המוגדר (לא רק ציר קבוע), ללא באגים של יציאה מגבולות השטח או "תקיעות" מצלמה.

- [ ] **התאמת נקודות ציון בשטח לתבנית הנוף בפועל**
  לוודא שכל נקודה ייחודית בניווט המודרך (פסגה, אוכף וכו') אכן נראית ככה גם בתוך הסביבה התלת-ממדית עצמה, ולא רק מסומנת כטקסט/מיקום.
  **הצלחה:** לכל נקודת-ציון מוגדרת (פסגה/אוכף/גיא/עיקול-נחל וכו') בוצעה בדיקה חזותית בתוך המודל התלת-ממדי שהיא אכן קריאה כאותה תבנית נוף מהזווית שהחניך רואה (למשל: פסגה נראית כגבוה-מקומי אמיתי ולא כישור/מדרון); נרשמה רשימת חריגות שתוקנו במודל/טרריין.

---

## סימולטור ניווט נוסף — תרגול בניית ציר ניווט (פיצ'ר חדש, שיעור ניווטים)

- [ ] **שלב 1 — זיהוי אלמנטים על מפה טופוגרפית לבנה**
  מציגים לחניך מפה טופוגרפית ("לבנה", ללא סימונים) ומבקשים לסמן אלמנטים חשובים לזיהוי (פסגה, אוכף, גיא, עיקול נחל).
  **הצלחה:** ממשק מאפשר לחניך לסמן נקודות על המפה ולתייג אותן כאחד מהאלמנטים; המערכת בודקת נכונות מול "מפתח" מוגדר מראש ומחזירה משוב לכל סימון (נכון/שגוי + הסבר).

- [ ] **שלב 2 — בניית ציר ניווט בין הנקודות**
  לאחר הזיהוי, החניך בונה ציר ניווט בין נקודות שנבחרו, והמערכת מפיקה פלט של סיפור דרך (אזימוט + מרחק + תיאור) בהתאם למוסכמות הקיימות בשיעור הניווטים.
  **הצלחה:** ניתן לבחור/לגרור נקודות ציר על המפה; המערכת מחשבת ומציגה אזימוט ומרחק אוטומטית בין נקודות עוקבות; מופק "סיפור דרך" טקסטואלי בפורמט התואם לתבנית הקיימת בשיעור 3/6 (אזימוט + מרחק בפתיח, ואז תיאור).

- [ ] **שלב 3 (אופציה) — הליכה בתוך שטח הניווט בתלת-ממד**
  להוסיף אופציה שהחניך "הולך" בפועל בתוך שטח הניווט שבנה, בדומה לסימולטור ניווט מודרך התלת-ממדי הקיים.
  **הצלחה:** ניתן לעבור ממצב תכנון (מפה 2D) למצב הליכה תלת-ממדי על אותו שטח/ציר שבנה החניך; נקודות הציון שנבחרו בשלב 1–2 מזוהות/מודגשות גם בתלת-ממד, בהתאם לדרישת ההתאמה שהוגדרה בסעיף הסימולטור התלת-ממדי לעיל.

- [ ] **שיבוץ בפרק הניווטים**
  התרגול צריך להיכנס כחלק מפרק/שיעור הניווטים בתרגול (ולא כשיעור נפרד).
  **הצלחה:** התרגול מופיע כסצנה/שלב בתוך שיעור הניווטים (לאחר שינוי המספור ל-6), נגיש מתפריט השיעור הרגיל, ועקבי בעיצוב (papercut/RTL/פלטת צבעים) לשאר סצנות השיעור.

---

## הנחיה מומלצת ל-Claude Code

```text
Use FIXES_2026-08-27.md as the source of truth for this batch of corrections.
Work top to bottom, section by section, starting with "כללי" (especially the
lesson-renumbering item, since it affects file paths/slugs referenced by later items).
For each issue you fix, change its checkbox from unchecked to checked and add a short
implementation note under the item, same convention as PROJECT_FIXES_TODO.md.
If an issue is only partially fixed, keep it unchecked and add a note starting with "Partial:".
Do not delete completed items. Preserve Hebrew wording unless a rewrite is explicitly required.
```
