Generated 2026-09-21 via Higgsfield (model: gpt_image_2_5). Replaces the isometric-diorama approach (mdo-diorama-scene.png) per design/proposals/mdo-ground-level/claude-handoff.md: a continuous ground-level scene with independent, fully-transparent per-domain layers instead of one baked diorama image with a dim-mask "disconnected" state.

Pipeline (same for all four mdo-*-object.png layers): (1) generate a full-scene composite by editing mdo-scene-background-ground.png (as the base/composition reference) with the original approved mdo-object-*.png product photo (as the design reference) added into the scene at the correct location — this reliably produces correct ground-level daylight lighting/perspective, which asking gpt_image_2_5 for an "isolated transparent cutout" directly did NOT (it kept reproducing the source photo's own dark studio vignette regardless of prompt). (2) crop tightly around just the added object from that composite. (3) run Higgsfield's background remover on the crop to get an isolated, correctly-lit transparent sprite. A naive pixel-diff against the clean background was tried first to extract the object and rejected — the regeneration isn't pixel-registered (sky/sea/vegetation texture differs everywhere, not just around the object), so diffing produced noise across the whole frame, not a clean object mask.

Composite-generation prompt (step 1, land): "Reference image 1 is a photographic Mediterranean coastal ground-level background plate: open sea on the left, a light dirt track curving through olive-green hillside scrubland toward the camera, blue sky with soft clouds, warm sun from the upper-left. Reproduce this exact background, composition, camera framing and lighting essentially unchanged. Reference image 2 shows an approved 8x8 armored military vehicle design (hull shape, turret, hatches, mirrors, antennas, olive-drab paint) to add into the scene. Add that same vehicle driving toward the camera along the dirt track in the lower-center-right of the frame, three-quarter front-left view, wheels visibly in contact with the dirt, a soft contact shadow beneath it, and a light dust trail behind the rear wheels, lit by the scene's own warm upper-left sunlight so it looks physically present in this photo, not pasted in. Keep the sea, sky, hillside, and every other part of the composition as close as possible to reference image 1. One single coherent photorealistic image. No text, no UI, no logo, no watermark."

Own contact shadow and dust trail are baked into this layer's own pixels (per the brief: every object's shadow/dust/trail must disappear with it) — no shared/global shadow layer is used anywhere in MDOScene.tsx.
