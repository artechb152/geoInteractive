# Assets needed — Topic 03 redesign

None identified so far. Every visual element across all 5 mockups (compass roses, topographic contours, coordinate-grid map furniture, terrain hachures, route diagrams, historical-example icons) can be built as hand-drawn inline SVG using the existing token palette, following the same conventions already used in this codebase (`PlanningScene.tsx`'s `RouteMap`/`Pine`/`Bush`/`Rock`/`Cairn` helpers, `OnboardingScene.tsx`'s `MissionStage` hill/palm illustrations, `CombatNavScene.tsx`'s `HandrailVisual`/`DeadReckoningVisual`/`PaceControlVisual`) or the existing `Icon`/`IconName` set.

This file will be updated with `ASSET NEEDED: <name>` placeholder entries if implementation reveals a genuine gap (e.g. a raster photo the mockup implies that can't reasonably be hand-drawn as SVG). None expected based on the design-spec analysis.
