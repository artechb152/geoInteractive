# Plan: topic-01 `#scene-levels` — replace comparison table with reference-matched selector

Branch: `redesign/topic01-history-stories` (continuation of today's `/reference-to-ui-exact` work on this branch — see `design/assumptions.md`, "Topic-01 onboarding" and "Topic-01 hook scene" sections, for the established conventions this plan follows).

## Global Constraints

- Do not modify any Hebrew/English copy anywhere in the file. If a diff shows a changed character inside a string literal that isn't whitespace-only formatting, that's a bug.
- Do not touch `src/components/lesson/SceneHeader.tsx` (shared component used site-wide) or the "תרגול גרירה" drag-and-drop practice section in the target file.
- Do not introduce new color tokens or hex values — only tokens already in `tailwind.config.ts`'s non-landing (`bg/fg/border/accent/status`) namespace. Do NOT use the `paper/olive/ember/pine/tanline` namespace (that's the landing-redesign palette used by a different section of this same page, `HistoricalCasesPanel.tsx` — not this one).
- No mirrored diorama image, no `left-`/`right-` literal Tailwind classes, no `text-align: left/right` — logical properties only (`ms-`/`me-`/`start-`/`end-`), per this project's `CLAUDE.md`.
- Dev server runs at `http://localhost:3001` (already running — do not start a second one; if the port is unavailable, report that rather than guessing).
- `npx playwright` is available on this machine (no local `playwright` devDependency) for the verification screenshot — use `npx --no-install playwright ...` or an ad-hoc Node script driving `playwright-core`/`playwright`, matching the pattern described in `design/assumptions.md` ("standalone Playwright script").
- Run `npx tsc --noEmit` before reporting done — a TypeScript/Next.js project; this task removes two functions and restructures JSX, so a type error here is a real regression, not noise.

## Task 1 — Rebuild the top card and verify against the reference

### Where this fits

`src/components/lessons/topic-01/LevelsScene.tsx` renders one scene (`#scene-levels`) inside the topic-01 lesson page. The scene currently opens with a shared `<SceneHeader>` (out of scope, do not touch), then a white `surface-elevated` card containing (a) a small pyramid diagram + explanatory paragraph, and (b) an HTML `<table>`-based comparison matrix (`ComparisonMatrix`) showing 3 levels of war (strategic/operational/tactical) × 4 dimensions (who decides / spatial zoom / time horizon / example). Below that card, unchanged, is a drag-and-drop practice exercise. This task replaces only the white card's contents with a new interactive selector matching a reference image the user supplied (`lesson1part3image1.png`, repo root, 1705×923px screenshot, not a clean 1:1 frame) and their explicit corrections to it, both below.

Read the full current file before starting: `src/components/lessons/topic-01/LevelsScene.tsx` (233 lines). Everything you need — `LEVELS`, `MATRIX_ROWS`, `LEVEL_ORDER`, the `Icon`/`IconName` import, `cn` helper — already exists there; reuse it, don't recreate it.

For a working precedent of the same "click a button, an active-item detail panel crossfades in" pattern already used elsewhere in this exact directory, read `src/components/lessons/topic-01/HistoricalCasesPanel.tsx` for structure/technique only (its own visual palette is the landing-redesign one — do not copy its colors, only its interaction pattern: `useState` for `activeId`, `AnimatePresence mode="wait"` crossfade, RTL column order via DOM order). Also read `src/components/assets/IsometricAsset.tsx` — the project's standard image-loading wrapper (never a broken-image icon; falls back to a placeholder only while the asset file doesn't exist on disk yet).

### Requirements (binding — from the approved design audit and the user's own corrections)

1. **Scope:** replace lines ~137–154 of `LevelsScene.tsx` (the `{/* === Comparison Matrix === */}` block: pyramid+paragraph wrapper, then `<ComparisonMatrix />`) with the new component described below. Then delete the now-unused `ComparisonMatrix` and `SVGPyramidStatic` function components further down the file (confirm via a repo-wide grep that neither is imported/used anywhere else first — they are not exported, so this should be a pure in-file removal).
2. **Delete the pyramid+paragraph block entirely** — do not relocate or shrink it, just remove it. (User-confirmed: it isn't in the reference and doesn't need to be preserved elsewhere.)
3. **Asset:** the diorama image is already on disk at `public/assets/lessons/topic01/scene-levels/TOPIC01-LEVELS-DIORAMA.png` (1536×1024 PNG) — do not move/rename it again. Load it through `IsometricAsset` (`assetId="TOPIC01-LEVELS-DIORAMA"`, Hebrew `alt`, `fit="cover"`), matching how `HistoricalCasesPanel.tsx` loads its own images. Pick whichever `aspect` value (or a custom container size, if none of `IsometricAsset`'s fixed aspect options — `16/9 | 21/9 | 1/1 | 4/3` — reads close enough) makes the rendered rectangle's proportions visually match the reference's image area; tune this by eye against the reference screenshot during the verification step, not by exact pixel math (the reference isn't a clean 1:1 frame).
4. **Layout — two columns inside the (still white/`surface-elevated`) card:** image with overlay buttons on the visual LEFT, active-level content panel on the visual RIGHT — this matches the reference and must NOT be mirrored just because the page is RTL. In this codebase's RTL convention (see `HistoricalCasesPanel.tsx`'s own comments), that means the content-panel column is the FIRST child in DOM (renders right / inline-start) and the image column is the SECOND child (renders left / inline-end).
5. **Buttons over the image:** 3 buttons, one per level, in `LEVEL_ORDER` (`strategic`, `operational`, `tactical` — first = right in RTL, per the existing comment on that array), each labeled with `LEVELS[id].label` (e.g. "אסטרטגית") — reuse the exact existing Hebrew strings, do not invent new button copy. Position them absolutely over the image at the approximate region that matches each level's real-world zoom scale on THIS SPECIFIC asset (top area ≈ strategic/widest view, middle ≈ operational, bottom-right vignette ≈ tactical closest view) — look at the actual rendered asset (not the reference photo, whose composition differs) and place by eye, refined in the verification step below. Clicking a button sets it as the active level.
6. **Color rule (user-confirmed, deliberately diverges from this same scene's pyramid/practice-bin sections below, which use a 3-color per-level identity):** only ONE state gets the orange (`accent`) treatment — whichever level is currently active (both its button and, if you use any active-state color in the panel, the panel). The other two levels' buttons stay neutral/dark (e.g. an existing dark surface + `text-fg`/muted tone) — do not use `accent-intel` (purple) or `terrain-sand` (sand) for this new component, even though those tokens exist and color-code the same three levels elsewhere in this file.
7. **Default active level on load:** `strategic` (matches the reference's default-expanded state and this file's own sibling pattern in `HistoricalCasesPanel.tsx`, which defaults to its first case).
8. **Right-side content panel — shows ONLY the active level's full info, not a 3-row list.** For the currently active level, render, in this exact order, one subheading + one paragraph per row, reusing `MATRIX_ROWS`' existing labels as the subheadings and `LEVELS[activeLevel][row.key]` as each paragraph, for all 4 rows: `מי מחליט?` (`who`), `זום מרחבי` (`zoom`), `אופק זמן` (`time`), `דוגמה מבצעית` (`example`). Also show the active level's own label/English name (`LEVELS[activeLevel].label` / `.english`) somewhere at the top of the panel. Every string must be the exact existing value from `LEVELS`/`MATRIX_ROWS` — no rewording, shortening, or re-ordering of the 4 rows.
9. **Animation:** crossfade the panel's content when the active level changes, using `framer-motion` (already a dependency, already used this way in `HistoricalCasesPanel.tsx` — match its `AnimatePresence mode="wait"` + short opacity/y transition style).
10. **Accessibility:** the 3 buttons and the panel should form a reasonable accessible pattern (e.g. `role="tablist"`/`role="tab"`/`aria-selected` on the buttons, `role="tabpanel"` + `aria-labelledby` on the panel, or an equivalent) — use your judgment, this isn't pixel-specified by the reference.

### Verification (mandatory — do not report done without this)

1. `npx tsc --noEmit` — fix any type errors.
2. Screenshot `http://localhost:3001/lessons/topic-01/#scene-levels` at 1440px width (this project's standard desktop target, per `CLAUDE.md`) with Playwright (`npx --no-install playwright ...` or an ad-hoc script — see Global Constraints). Compare against `lesson1part3image1.png`, specifically the image+buttons+panel region (the reference's header above it is out of scope and will legitimately look different, since `SceneHeader` wasn't touched).
3. Click through all 3 buttons and screenshot each state. Check specifically: image is not mirrored/flipped; orange appears only on the active button (and optionally panel accent), never on the other two; no clipped/cut-off text in the panel for the longest `example` string among the 3 levels; RTL column order is correct (image visually left, panel visually right, confirmed in the actual screenshot, not just in the JSX).
4. Fix any material mismatch found and re-screenshot until clean. A named, specific limitation is acceptable to report as remaining (e.g., "button placement is an eyeballed approximation since the reference photo and the new asset have different compositions — buttons sit correctly over the new asset's own regions") — a silent, unverified claim of "matches the reference" is not.
5. Append a dated entry to `design/assumptions.md` (reuse the existing "## Topic-01 onboarding — ..." section's dated-subentry style, or add a small new `##` heading if that fits better — use judgment) documenting: the deliberate single-color-active decision and why it diverges from the rest of this scene, the deleted pyramid block, and that button placement was tuned by eye against the actual rendered asset (not measured against the reference's own non-1:1 screenshot).
6. Append one line to `docs/UI-CONSISTENCY-RECOMMENDATIONS.md` under `## Screen-specific findings` (new `### topic-01 #scene-levels` subsection) noting that this screen intentionally does NOT use the tri-color per-level identity used elsewhere in the same file, so a future consistency pass doesn't "fix" it back without checking this decision first.
7. Commit the change. The asset file at `public/assets/lessons/topic01/scene-levels/TOPIC01-LEVELS-DIORAMA.png` is already on disk (copied by the controller before this task started) — include it in this commit if `git status` shows it untracked/unstaged.

### Report

Write your full report to the report file path given in your dispatch. Include: what you implemented, the `tsc` result, each screenshot taken and what it showed, every delta found and how it was fixed, anything left unresolved and why, and the exact commit hash(es). Then reply with the short status contract from your dispatch instructions.
