/* ============================================================================
 *  pyramidGeometry.js — מידות הפירמידה התלת־ממדית (Three.js)
 * ----------------------------------------------------------------------------
 *  כל שכבה היא קטום־חרוט מרובע (CylinderGeometry עם 4 פאות):
 *  rBot = רדיוס הבסיס · rTop = רדיוס הפסגה · h = גובה · y = מרכז אנכי בסצנה.
 *
 *  לשכבה העליונה (אסטרטגית) rTop = 0 — ולכן היא חרוט מרובע, כלומר קודקוד
 *  פירמידה אמיתי. שלוש השכבות יחד יוצרות פירמידה מלאה ורציפה (rBot של שכבה
 *  עליונה = rTop של השכבה שמתחתיה), הצבועה בשלושה צבעי דרגה.
 *
 *  המידות משותפות לפירמידה הראשית ולמוקטנת — אותה שפה ויזואלית.
 * ========================================================================== */

export const PYRAMID_TIERS = {
  strategic: { rTop: 0.0, rBot: 0.55, h: 0.74, y: 0.74 },
  operational: { rTop: 0.55, rBot: 1.05, h: 0.74, y: 0.0 },
  tactical: { rTop: 1.05, rBot: 1.55, h: 0.74, y: -0.74 },
}

// סדר מלמעלה למטה (אסטרטגית → טקטית).
export const TIER_ORDER = ['strategic', 'operational', 'tactical']

// מפלס הקרקע (תחתית הבסיס) — לצל המגע.
export const GROUND_Y = -1.11
