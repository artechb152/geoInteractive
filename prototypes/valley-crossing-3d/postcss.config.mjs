// Local, empty PostCSS config — stops Next.js from walking up to the host
// site's postcss.config.mjs and running Tailwind here (this prototype does
// not use Tailwind).
export default {
  plugins: {},
};
