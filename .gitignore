node_modules/
.next/
out/
.env*
!.env.example
*.log
.DS_Store
dist/
scorm-package/
*.zip

# Local export folder used to share specific topics — not part of the runnable app
topics-1-2-review/

# Claude Code — commit shared skills/agents, ignore local machine state
.claude/settings.local.json

# External prototype sources (cloned from idog2210/Terrain3DSimulator…
# and idog2210/TerrainOverlay… for in-app embedding). Their built
# output lives at public/prototypes/<name>/ and IS tracked. The source
# trees stay external — to rebuild after upstream changes:
#   npm run prototypes:build
# Leading slash anchors this to the repo root so it doesn't match
# public/prototypes/ as well.
/prototypes/

# QA render artifacts (throwaway PNGs from scripts/qa/render-svg.mjs)
qa-output/

# TypeScript incremental build info
*.tsbuildinfo

# Playwright MCP session logs and ad-hoc QA screenshots
.playwright-mcp/
/geo09-*.png
# Ad-hoc QA render screenshots at repo root (throwaway). referenceImageDesign.png
# is the design target and stays tracked — none of these patterns match it.
/design-approval-*.png
/cards-grid*.png
/card-*.png
/qa-verify-*.png
/mobile-*.png
